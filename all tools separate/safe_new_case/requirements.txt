# Runtime dependencies - SAFE Project
# Install with: pip install -r requirements.txt

# --- Core data ---------------------------------------------------------------
pandas==2.3.3

# --- UI ----------------------------------------------------------------------
gradio==6.12.0           # Chat UI  (app_v3.py)
streamlit==1.53.1        # Demand Discovery Workspace (case2_workspace_v1.py)
plotly==6.7.0            # Charts inside Streamlit workspace

# --- LLM / LangChain ---------------------------------------------------------
langchain-core==1.3.0
langchain-google-genai==4.2.1
langchain-ollama==1.1.0

# --- Document generation -----------------------------------------------------
python-docx==1.2.0

# --- Environment / secrets ---------------------------------------------------
python-dotenv==1.0.1

# --- Database (migration only) -----------------------------------------------
# Only required when running migrate_to_sqlite.py to copy data
# from a MySQL source. Skip entirely if data/saas_data.db already exists.
SQLAlchemy==2.0.46
PyMySQL==1.1.2
